<UserControl x:Class="WpfApp1.UsersControlls.OneControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.UsersControlls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock></TextBlock>
            <DockPanel>
                <Button DockPanel.Dock="Left" Content="Button 1" Command="{Binding commandForButton1}"></Button>
                <Button DockPanel.Dock="Right" Content="Button 2" Command="{Binding commandForButton2}"></Button>
                <TextBlock Text="{Binding Str}" TextAlignment="Center" VerticalAlignment="Center"></TextBlock>
            </DockPanel>
        </StackPanel>
    </Grid>
</UserControl>
